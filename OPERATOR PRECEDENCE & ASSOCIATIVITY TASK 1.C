#include <stdio.h>

int main() {
    int result;
    
    // The expression to be evaluated
    result = 5 + 2 * 3;

    // Display the result
    printf("The expression is: 5 + 2 * 3\n");
    printf("The result is: %d\n", result);

    return 0;
}